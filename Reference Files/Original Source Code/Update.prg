set deleted on
set century on

SELECT A
USE c:\americo\PO_HEAD INDEX c:\americo\PO_HEAD
set filter to ship_date > ctod('06/30/2021')
goto top

SELECT B
USE REM_SUP
INDEX ON RSUPCODE TO RSUPCODE
USE REM_SUP INDEX RSUPCODE

SELECT A
DO WHILE .NOT. EOF()
   SELECT B
   SEEK A->RSUPCODE
   IF .NOT. EOF()
       REPLACE USED WITH 'Y'
   ENDIF
   SELECT A
   SKIP
ENDDO

SELECT B

SELECT A
GOTO TOP

USE SUPPLIER
INDEX ON SUP_CODE TO SUP_CODE
USE SUPPLIER INDEX SUP_CODE

SELECT A
DO WHILE .NOT. EOF()
   SELECT B
   SEEK A->SUP_CODE
   IF .NOT. EOF()
       REPLACE USED WITH 'Y'
   ENDIF
   SELECT A
   SKIP
ENDDO

CLOSE ALL
